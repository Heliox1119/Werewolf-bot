# Discord Bot Configuration
TOKEN=your_discord_bot_token_here
CLIENT_ID=your_application_client_id
# GUILD_ID is optional — if set, commands are also registered as guild commands (instant)
# If omitted, only global commands are registered (~1h propagation)
GUILD_ID=your_discord_server_id

# Logging Configuration
# Levels: DEBUG (most verbose), INFO (default), WARN, ERROR, NONE (silent)
# DEBUG: All details including timers and technical info
# INFO: Normal operations and successes (recommended for production)
# WARN: Only warnings and errors
# ERROR: Only errors and critical issues
# NONE: Disable all logging
LOG_LEVEL=INFO

# Bot Owner (your Discord user ID — gets automatic lifetime premium)
OWNER_ID=your_discord_user_id

# Web Dashboard — Session Secret (optional)
# If not set, a secret is auto-generated and persisted in data/.session-secret
# Set this to override with your own value
SESSION_SECRET=

# Monitoring Configuration (optional)
# Discord webhook URL for monitoring alerts
# Create webhook: Server Settings → Integrations → Webhooks → New Webhook
# Copy webhook URL and paste below
# Leave empty to disable alerts (metrics will still be collected)
MONITORING_WEBHOOK_URL=
